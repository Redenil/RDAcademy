@model IEnumerable<RDAcademy.Models.Contract>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CodeContract)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EffectDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Individual.LastName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CodeContract)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EffectDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Individual.LastName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
